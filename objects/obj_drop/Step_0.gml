
/*
//follow the player once dropped
if (instance_exists(obj_player)) { 
   if (distance_to_object(obj_player) < 40) { // 40 = distance 
   _direction = point_direction(x, y, obj_player.x, obj_player.y); 
   phy_speed_x = lengthdir_x(2, _direction); // 2 = xspeed 
   phy_speed_y = lengthdir_y(2, _direction); // 2 = yspeed 
   } else { 
      phy_speed_x = 0; 
      phy_speed_y = 0; 
      } 
   }
   */

/* */
/*  */
